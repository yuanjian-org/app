import colors from "./colors";

const links = {
	components: {
		Link: {
			baseStyle: {
        color: colors.brand.c,
				_hover: {
					textDecoration: 'none',
				},
			},
		}
	}
};

export default links;