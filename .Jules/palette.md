## 2026-07-07 - Add show/hide password toggle to login page
**Learning:** Added a show/hide password toggle using Chakra UI's `InputRightElement`, `IconButton`, and `ViewIcon`/`ViewOffIcon` to improve accessibility and usability on the login page. Ensure the `aria-label` attribute matches the application's primary locale (Chinese).
**Action:** Always verify if a password field lacks a toggle button to improve UX, and ensure proper ARIA attributes are set in the correct locale.
