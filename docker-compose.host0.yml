services:
  demo:
    image: ${APP_IMAGE_BASE_NAME:?}.demo:latest
    container_name: demo
    env_file: .env.demo
  ustc:
    image: ${APP_IMAGE_BASE_NAME:?}.ustc:latest
    container_name: ustc
    env_file: .env.ustc
  xhef:
    image: ${APP_IMAGE_BASE_NAME:?}.xhef:latest
    container_name: xhef
    env_file: .env.xhef
  nginx:
    image: m.daocloud.io/docker.io/library/nginx:latest
    container_name: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx.proxy.conf:/etc/nginx/proxy_params.conf:ro
      - ./nginx.ssl.conf:/etc/nginx/ssl_params.conf:ro
      - ./certbot:/var/www/certbot:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "443:443"
      - "80:80"
    depends_on:
      - demo
      - ustc
      - xhef
