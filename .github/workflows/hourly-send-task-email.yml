name: Hourly Send Tasks And Emails

permissions: {}

on:
  schedule:
    - cron: "10 * * * *"
  workflow_dispatch:

jobs:
  run_scheduled_task:
    uses: ./.github/workflows/run-curl.yml
    with:
      cron_tasks: "cron.createAutoTasks cron.sendScheduledNotifications"
    secrets: inherit
